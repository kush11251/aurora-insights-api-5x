# Aurora Insights API
A high-performance data analytics and insights engine.
## Getting Started
1. Run `go mod init aurora-insights-api` to initialize the Go module.
2. Run `go get` to install dependencies.
3. Run `go build src/main.go` to compile the code.
4. Run `go run src/main.go` to execute the code.